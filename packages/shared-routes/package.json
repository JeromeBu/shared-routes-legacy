{
  "name": "shared-routes",
  "publishConfig": { "access": "public" },
  "type": "module",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "rm -rf ./dist && tsc -p tsconfig.build.json",
    "lint": "eslint src --fix"
  },
  "devDependencies": {
    "config": "workspace:*",
    "eslint": "^8.11.0",
    "tsconfig": "workspace:*",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "zod": "^3.11.6"
  }
}
